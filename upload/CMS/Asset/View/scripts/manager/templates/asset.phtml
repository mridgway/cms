<li id="asset-${id}" class="asset">
    <img src="${thumb}" alt="${name}" />
    <h3 class="name">${name}</h3>
    <h4 class="type">${type}</h4>
    <h4 class="upload_date">${upload_date}</h4>
    <ul class="actions">
    </ul>
    <div class="action-box"></div>
</li>